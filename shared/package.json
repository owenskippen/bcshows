{
  "name": "bcshows-shared",
  "version": "0.1.0",
  "description": "Shared types and utilities for BC Card Shows",
  "main": "types/show.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
